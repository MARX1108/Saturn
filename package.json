{
  "name": "saturn",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn workspace @saturn/server start",
    "dev": "yarn workspace @saturn/server dev",
    "test": "yarn workspace @saturn/server test",
    "build": "yarn workspace @saturn/server build",
    "lint": "yarn workspace @saturn/server lint"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "eslint": "^8.0.0",
    "prettier": "^3.0.0"
  }
}
